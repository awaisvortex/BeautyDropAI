# Cloud Build configuration for automatic deployment to Cloud Run (STAGING)
steps:
  # Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:$COMMIT_SHA"
      - "-t"
      - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:latest"
      - "."

  # Push the image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:$COMMIT_SHA"

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:latest"

  # Deploy to Cloud Run (Staging)
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "beautydrop-api-staging"
      - "--image"
      - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:$COMMIT_SHA"
      - "--region"
      - "us-east1"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--min-instances"
      - "0"
      - "--max-instances"
      - "10"
      - "--timeout"
      - "300"
      - "--port"
      - "8080"
      - "--update-env-vars"
      - "DJANGO_SETTINGS_MODULE=config.settings.production"

# Store images in Artifact Registry
images:
  - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:$COMMIT_SHA"
  - "us-east1-docker.pkg.dev/$PROJECT_ID/beautydrop-django/app-staging:latest"

options:
  logging: CLOUD_LOGGING_ONLY
